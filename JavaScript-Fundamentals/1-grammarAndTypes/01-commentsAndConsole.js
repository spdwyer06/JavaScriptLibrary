/*
    Block comment (Multi-Line comment)
*/

// Single line comment

/*
Shortcuts:
    Code Runner short cut (control+alt+n)  
    Open in browser shortcut (alt+b)
    Comment shortcut (shift + /)
*/

/*
Value of a comment:
    communicates to anyone reading your code
*/

/*
console.log
    outputs some message to the console
*/

// Ctrl + Alt + N runs code
// You can select specific lines to run just the highlighted code

console.log('This is a console.log() message.');

// With a comma it automatically adds a space between the strings
console.log('You can separate parts of a console message with commas,', 'just like this.')
console.log(13, 15, 17, 18);

// console.log and debugging
// we can debug with console.log messages in 2 big ways:
// 1) check the status of some JS 'thing'
// 2) check a process we have built with repeated console.log messages

// Example 1:
var sampleName = 'Sean';
console.log(sampleName);

// Example 2:
var sampleNum = 1;
sampleNum++;
sampleNum++;
console.log(sampleNum);
sampleNum++
console.log(sampleNum);
sampleNum++;
console.log(sampleNum);